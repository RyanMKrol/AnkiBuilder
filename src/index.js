/**
 * AnkiBuilder entrypoint (placeholder scaffold).
 *
 * The real pipeline — data contracts, corpus assembly, translation, audio, deck
 * build — lands in later tasks (T002+). For now this just proves the toolchain
 * (format / lint / test / build) is wired and green.
 */
export function hello() {
  return "anki-builder";
}
