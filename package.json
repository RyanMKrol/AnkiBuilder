{
  "name": "anki-builder",
  "version": "0.0.0",
  "description": "A tool for building Anki flashcard decks.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "anki-builder": "./src/cli/bin.js"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "node --test",
    "build": "node -e \"process.exit(0)\""
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3"
  }
}
