## Do

Lay down the minimal Node project skeleton so an empty build goes green through the full
Definition of Done. Mechanical boilerplate — no Anki logic yet. Use exactly this toolchain so
the scope stays tight:

- `package.json` — name `anki-builder`, `"type": "module"`, entrypoint `src/index.js` (a trivial
  module is fine for now). Add these `scripts`, each of which must run and exit 0 on the skeleton:
  - `format:check` → `prettier --check .`
  - `lint` → `eslint .`
  - `test` → `node --test`
  - `build` → a step that exits 0 (e.g. `node -e "process.exit(0)"` as a placeholder until there's
    something to bundle)
- `.prettierrc.json` and `.prettierignore` — minimal Prettier config.
- `eslint.config.js` — minimal ESLint flat config that passes on the skeleton.
- `src/index.js` — a trivial entrypoint.
- `test/smoke.test.js` — one trivial passing test using `node:test`.
- `.env.example` — a committed template documenting the environment variables the tool reads
  (at minimum `ELEVENLABS_API_KEY=`), with a comment that users copy it to `.env` (which is
  gitignored) and that secrets are read from the environment only, never written to disk. NOTE: a
  starter `.env.example` may already exist in the repo — keep/extend it rather than deleting it. The
  `.gitignore` already carries a `!.env.example` negation so the file is tracked; leave `.gitignore`
  as-is (do not edit it — it is outside this task's scope).
- Commit the generated `package-lock.json` so CI's `npm ci` works.

## Done when

`npm ci`, `npm run format:check`, `npm run lint`, `npm test`, and `npm run build` all pass
locally; the CI workflow (`.github/workflows/ci.yml`) is green on the pushed branch; `README.md`
documents how to install and run.
